# =============================================================
#  Underscore — Python dependencies
#
#  Python 3.9 – 3.12 (Windows). PyTorch is installed separately
#  by setup.ps1 (CPU build by default; run `setup.ps1 -Gpu` for
#  an NVIDIA CUDA build). Then install this file with:
#      pip install -r requirements.txt
# =============================================================

# Web server
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# AI source separation (Meta Demucs)
demucs==4.0.1
soundfile>=0.12.1

# Job control — pause / resume / cancel of running Demucs processes
psutil>=5.9

# NumPy 2.x breaks torch 2.3.x interop — keep 1.x
numpy<2